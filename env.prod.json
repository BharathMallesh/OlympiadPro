{
  "_comment": "Production/staging. Set API_BASE_URL to the deployed backend (no trailing slash). Use with: flutter build apk --release --dart-define-from-file=env.prod.json",
  "API_BASE_URL": "https://your-backend.onrender.com"
}
