import 'app/flavor.dart';
import 'main_common.dart';

/// Vidyora Educator — the teacher/validator app (authoring, grading, analytics).
/// Build: flutter run --flavor teacher -t lib/main_teacher.dart
void main() => bootstrap(AppFlavor.teacher);
